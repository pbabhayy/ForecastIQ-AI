"""
Config: Constants
=================
Application-wide constant values and enumerations.

RESPONSIBILITY:
    - App name/tagline, page identifiers and labels.
    - Canonical field names (date, revenue, orders, customers, expenses).
    - Column-mapping synonym dictionaries (source header -> canonical).
    - Session-state keys, health-score band labels, supported file types.

Eliminates all magic strings/numbers across the codebase.
"""
